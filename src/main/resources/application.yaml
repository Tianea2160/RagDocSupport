spring:
  application:
    name: RagDocSupport
  ai:
    mcp:
      server:
        name: rag-doc-support
        version: 1.0.0
        type: SYNC
        protocol: STREAMABLE
    ollama:
      base-url: http://localhost:11434
      embedding:
        model: nomic-embed-text

server:
  port: 8080

qdrant:
  host: localhost
  port: 6334
  collection-name: doc-chunks

crawler:
  sleep-time-ms: 100
  timeout-ms: 30000
  retry-times: 2
